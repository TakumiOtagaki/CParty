# Handoff (2026-02-17 16:30)

## 目的
- SCFG rules_core / applicable_rules の整備と fixed-structure 側への段階接続。
- PF 側は `SCFG_RULES_MODE=1` + `SCFG_RULES_APPLICABLE=1` で既存 strict compare を通す運用を維持。

## 今回の進捗サマリ
### 1) rules_core の適用経路（applicable_rules）
- `SCFG_RULES_APPLICABLE=1` を追加し、PF の rules_runtime を applicable_rules 経由に切替可能に。
- 既存の strict compare で **legacy_debug と全件一致**を確認。

### 2) WM ルール列挙の補正
- `WM_SPLIT_*` が `k==i` を二重計上していたため、`k>i` のみに限定。
- Sample_WM の backtracking 失敗を解消。

### 3) RuleSpec / SplitSpec のメタ拡張
- `SplitSpec` に `uses_p/uses_q` を追加。
- `rule_spec(RuleId)` で RuleSpec を参照可能に。

### 4) fixed_energy への rules_core 互換トレース（slice A〜D）
- `trace_rule_chain_slice_*_rules_core` を追加。
- 既存 slice テストに rules_core trace の整合チェックを追加。
- slice A/B/C/D テストはすべて実行・OK。

### 5) テストリンク修正
- fixed_energy 系テストに `scfg` / `sparse_tree` / `rules_part_func` をリンクするよう CMake を更新。

## 重要なテスト結果
### PF strict compare
```
SCFG_RULES_MODE=1 SCFG_RULES_APPLICABLE=1 \
  test/tools/compare_cli_stdout.sh worktree_legacy_debug/build/CParty build/CParty 42 200 30,50,80
```
- `compare_matched=600`
- `compare_failed=0`

### fixed_energy slice tests
```
build/fixed_energy_slice_a_test  # ok
build/fixed_energy_slice_b_test  # ok
build/fixed_energy_slice_c_test  # ok
build/fixed_energy_slice_d_test  # ok
```

## 主なコミット
- `fix: align WM rule splits with sampler`
- `feat: add applicable rules path for rules runtime`
- `feat: enrich rule spec metadata`
- `feat: add rules_core slice-a trace scaffolding`
- `feat: add rules_core slice-b trace scaffolding`
- `feat: add rules_core slice-c trace scaffolding`
- `feat: add rules_core slice-d trace scaffolding`

## 追加した/更新した主要ファイル
- `src/scfg/rules_core.cc/.hh`
- `src/scfg/rules_runtime.cc`
- `src/scfg/rules_engine.cc/.hh`
- `src/fixed_energy_api.cc/.hh`
- `test/fixed_energy_slice_a_test.cc`
- `test/fixed_energy_slice_b_test.cc`
- `test/fixed_energy_slice_c_test.cc`
- `test/fixed_energy_slice_d_test.cc`
- `test/tools/compare_cli_from_tsv.sh`
- `CMakeLists.txt`

## 現在の状況
- PF 側: rules_core + applicable_rules 経由で strict compare 完走。
- fixed_energy 側: slice A〜D で rules_core 互換トレース整備済み。
- 次フェーズ: fixed_energy の rule selection を rules_core 由来に置換する段階。

## 次にやること
1. fixed_energy_api の deterministic rule selection を rules_core に置換（段階的に）。
2. 置換後も既存 slice テストを継続して pass させる。
3. PF 側 strict compare を毎回併走（`SCFG_RULES_MODE=1` + `SCFG_RULES_APPLICABLE=1`）。

## 備考
- `worktree_legacy/` と `worktree_legacy_debug/` は未追跡のまま触っていない。
- slice テストは `ctest` に未登録のため、バイナリ直接実行で検証。
